{
  "name": "holaday-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "HOLA DAY — Browser Agent monorepo (Phase 0)",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "qa:web": "pnpm --filter @holaday/orchestrator qa:web",
    "typecheck": "turbo run typecheck",
    "lint": "biome check .",
    "test:ops": "node --test ops/aliyun-edge/*.test.mjs ops/cloudflare-geo-redirect/*.test.mjs && bash scripts/deploy-rollback-target.test.sh && bash scripts/orchestrator-runtime.test.sh && bash scripts/deploy-migration-gate.test.sh",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/global-agent": "^3.0.0",
    "rimraf": "6.0.1",
    "turbo": "2.5.8",
    "typescript": "5.7.3"
  }
}
