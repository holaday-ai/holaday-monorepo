# 图片工作室视觉收口 QA

## 对照基线

- 设计源：`/Users/yaleiqi/holaday-monorepo/.worktrees/image-creation-studio-design/docs/superpowers/specs/assets/2026-08-28-image-creation-studio-goal-first.png`
- 设计源尺寸：1680 × 944；主内容同屏裁切：1280 × 720，偏移 `(400, 0)`
- 实现视口：1280 × 720，桌面 1x
- 对照状态：`锁定主角`，主角图已就绪，提示词已填写
- 实现截图：`/private/tmp/holaday-image-ready-state-final.png`
- 同屏对照：`/private/tmp/holaday-image-final-ready-compare.png`
- 空状态与页面下半区：`/private/tmp/holaday-image-qa-lock-v3.png`、`/private/tmp/holaday-image-qa-lower.png`

## 回归记录

### 第一轮

- P1：页面标题重复，英文品牌块和“图片创作”再次占据首屏。
- P1：生成设置与主操作脱离创作区，流程视觉上被切断。
- P1：目标卡只有低信息量裁切图，页面下方仍是文字说明，没有展示连续创作能力。

处理：恢复单一“图片任务”标题；将设置、校验与开始生成收进同一创作区；按三个目标生成专用横幅资产；补充连续创作三联图。

### 第二轮

- P2：目标卡比例、选中标记位置和创作区间距与设计源仍不一致。
- P2：锁定主角上传完成后仍显示紧凑附件条，不能成为画面的视觉锚点。

处理：统一卡片高度、圆角和标记位置；主角上传完成后改为大图预览，提供明确的移除与更换入口；更换新图会真实替换当前主角，不保留误导性的旧锚点。

### 第三轮

- 同屏复核未发现 P0、P1 或 P2 视觉差异。
- P3 可接受差异：生产使用全新生成的虚构人物和无品牌商品图，不复制设计稿中的具体人物或商标内容；页面结构、比例、层级、色彩角色和关键交互保持一致。

## 交互与质量检查

- 三个创作目标可切换，商业成片会展示三种用途。
- 设置弹层可打开和关闭；自动化测试覆盖 Escape 后焦点恢复。
- `锁定主角`空态、已就绪态、移除和更换入口均有可访问名称；图标按钮同时包含 `aria-label` 与原生 `title`。
- 主角替换遵守五张附件边界，并回收旧 blob 预览 URL。
- 四张生成资产从 7,162,927 bytes 压缩为 1,142,250 bytes，下降约 84%，未见可感知画质损失。

## 结果

passed
