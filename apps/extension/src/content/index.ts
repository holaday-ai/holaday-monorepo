/**
 * Content script — Phase 0 PoC A1 placeholder.
 *
 * Real DOM I/O lives in the Playwright-CRX adapter (W2+). For now we only
 * tag the page so logs prove the script loaded.
 */
console.debug('[holaday] content script loaded on', window.location.href);
