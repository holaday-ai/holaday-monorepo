# 已批准方案交付与回复恢复修复

## 真实发现

PR231 的单账号 API 探针验证了状态、区域和持久化字段，但其 completed 结果仍以“请确认以上方案……确认后我将输出最终的简洁执行清单”结尾。因此此前状态通过不能算最终内容质量通过。浏览器刷新后只显示初始需求，修订和确认回复也未恢复。

本轮只读检查了同一既有合成任务，没有新建生产任务或重开灰度。生产仍为 PR231 `107857fe70503e30691073f267d87275596edb20`；未改变生产配置。

## 实施范围

- 路由显式向生成器传递已核验的执行阶段，不把历史“先别执行”当作当前等待状态。原方案/附件始终是不可信用户级参考；工具和外部动作授权不扩大。
- 已批准执行不进入轻量问答短路。对实证中的重复批准末尾提示增加窄范围矛盾检查；不宣称这是通用质量、事实或安全核验器。
- 完成结果保存有限的方案回复历史及修订方案，详情读取恢复既有对话气泡。旧生产结果缺失的历史不会被回填或伪造。
- 回复写入开始时废弃更早的详情读取；待全部写入结束后重新读取，避免猜测文本重叠或删除重复“确认”。执行中旧 JSON 不恢复为新历史。失败本地回复保留；会话切换清理临时记录。

## 独立审查和红绿证据

已复现并修复：合法负责人依赖被误拦、引用中的第一人称被误拦、首次加载覆盖新回复、服务器快照已含新回复导致重复、最后写入失败导致历史读取丢失。针对性测试为后端81项、前端110项，全部通过。前后端完整类型检查通过。

保留边界：传输失败但服务器实际已保存时，未确认写入的本地消息仍可能与服务端历史同时显示；后续应区分送达不明状态。该问题不改变后端批准权限。不得用文本去重删除真实重复回复。

## 浏览器验证

- Chrome，默认桌面视口1496×1007，本地 `http://127.0.0.1:5192/probe.html`。
- 真实 TaskStream、task-store 和生产样式；只有详情传输与结果使用固定合成数据，没有模型或生产 API 请求。
- 正常加载、刷新、展开处理思路；修订600元和确认气泡各1次，最终清单可见，无白屏或框架错误覆盖，控制台 error/warn 为空。
- 临时预览、标签页和会话已关闭。未测试移动端、附件联动或此修复的真实生产模型效果；本地显示验收不是生产全链路通过。

## 发布门禁

- [x] 针对性红绿回归、类型检查、独立审查、真实组件刷新验收。
- [x] 完整串行回归、发布合同及构建。
- [ ] PR 创建及最终审查。
- [ ] 新版本精确发布/灰度工具、独立备份与回滚验证。
- [ ] 单账号真实模型重新交付、持久化回读与浏览器刷新；结束恢复 dark。

真实目标分支为 `claude/musing-keller-ae1d05`，不是 main。当前仓库没有该版本的 GitHub Actions 记录，不得把空 CI 列表当作通过。所有本机重任务串行、单 worker、有限 Node 堆；不启动 Docker、安装或多浏览器。

关联待处理：PR231 自动审查还提到“批准后保留原 workflowOverride 决定”和“纯暂停附带附件不应丢附件”两个既有问题。本补丁不声称解决这两项，附件/技能联动放行前需单独复现并修复。

## 完整验证过程记录

- 后端最终覆盖395文件、6153项测试：单worker/forks/1GB组通过393文件6133项；两项较重的既有静态治理审计在独立串行2GB组通过2文件20项。没有跳过测试或放宽断言，未运行需要数据库的 integration 配置。
- 首次整批从仓库根目录启动，mysql-result 扫描受 cwd 影响失败，并有2个1GB worker OOM；修正目录后仍有这两项堆限制退出。失败记录保留，不能称为“一次完整命令全绿”。
- 日志位于 `/private/tmp/holaday-approved-plan-backend.log`、`holaday-approved-plan-backend-forks.log`、`holaday-approved-plan-governance.log`。
- 修改文件 Biome lint 保留 tasks.ts 的3项既有 non-null assertion 错误；已对照HEAD确认位于原有幂等创建逻辑，与本次diff不重叠。未做大文件格式重写。生成器3文件格式已通过，git diff --check通过。
- 同轮只读健康检查：两个生产healthz均200/status ok。不等同于新版本已部署。
- 前端完整回归249文件2426项通过；两端类型检查、后端build、前端全量eslint与build通过。前端build保留已有大于700kB的chunk提示。
- QA数据库安全/报告/发布合同和Qwen合同共78项通过，Qwen静态发布合同通过。没有执行数据库迁移、Docker集成测试或真实供应商调用。
- 前端及合同日志：`/private/tmp/holaday-approved-plan-frontend.log`、`holaday-approved-plan-frontend-lint.log`、`holaday-approved-plan-frontend-build.log`、`holaday-approved-plan-contracts.log`、`holaday-approved-plan-qwen-contract.log`。单次检查的通过仅对应本轮冻结的代码，不替代待完成的发布/模型门禁。
