# HOLA DAY — Daily Report · 2026-04-22 (Day 7 · W1 收官)

> **Branch**：`claude/chrome-extension-poc-biwr6`
> **Phase**：0 (W1 完成；W2 本周一 04-28 启动)
> **本日聚焦**：真 Opus × 6 次彩排，3 个 bug 全修，出 W1_REHEARSAL.md + W1 收官总结。

---

## 今日交付（Day 7）

| # | 任务 | Commit |
|---|---|---|
| 1 | 真 Opus 4.7 × 6 次跑 Demo A/B/C（彩排 harness 在 `apps/orchestrator/.rehearsal/`，gitignored，不进 repo） | — |
| 2 | 3 个 bug 修复（`thinking + tool_choice` 冲突 / `tasks.create` 漏广播 / selector zod 过严） | `b381cbc` |
| 3 | `docs/W1_REHEARSAL.md`（6/6 pass 表 + 延迟 + bug 根因 + Opus vs Stub + W2 backlog 5 项） | `b381cbc` |

**6 次彩排全部通过**。真 Opus 产 plan 7-11 步，延迟 10-15s，单次成本 ~$0.025，6 次总共 ~$0.15 < 预算 $0.50。

---

## W1 7 天产出清单（04-17 → 04-22 完整列）

### Commits（25 次，Day 0 到 Day 7）

| Day | 日期 | commit | 摘要 |
|---|---|---|---|
| 0 | 04-17 PM | `efcca74` | 项目文档落地（设计 3 件） |
| 1 | 04-17 EOD | `d4a05e5 … e733567` | monorepo + docker-compose + orchestrator 骨架 + 8 表 DDL + shared-types + 日报 |
| 2 | 04-18 | `8d20ef4 … 963945d` | partition rollover EVENT / Auth 流 / `apps/extension` MV3 / TaskController + WS control loop / 集成测试 / 日报 |
| 3 | 04-18 晚 | `c811544 … aa9a3c3` | Commander（Anthropic planner + `tasks.create`） / TaskController 持久化 / Skill SDK + 首个 skill / `pnpm dev` + Chrome 指南 / 日报 |
| 4 | 04-19 | `e4e4c46 … edccf14` | Anthropic Haiku smoke / `pending_confirm_payload` + `rehydrate()` / restart recovery / 日报 |
| 5 | 04-20 | `c82f979 … 25a3b1d` | Commander skill catalogue 注入 / paused 状态机三触发器 / 日报 |
| 6 | 04-21 | `00d79aa … ff1a7e1` | eastmoney-news-digest skill + 战略 pivot 记录 / popup Pause/Resume/Confirm + tasks.confirm tRPC / W1_DEMO.md / 日报 |
| 7 | 04-22 | `b381cbc` | 彩排 + 3 bug 修 + W1_REHEARSAL.md |

### 代码交付

```
apps/
  orchestrator/          21 TS + 3 SQL migrations + 2 scripts
    src/agent/             planner / task-controller / task-repository / planners/{stub,anthropic}
    src/auth/              password / jwt / service / middleware
    src/skills/            registry.ts
    src/trpc/              context / router / trpc + routers/{auth, tasks (+pause/resume/confirm)}
    src/ws/                server.ts (broadcaster + rehydration)
    src/db/schema/         users / user_profiles / skills / sessions / tasks / task_steps / task_events / llm_calls
    drizzle/               0000_initial / 0001_pending_confirm_payload / 0002_paused_state + manual/
    scripts/               smoke-anthropic.ts / sync-skills.ts
  extension/             8 TS / TSX
    src/background/        SW + ws-client + stub step execution
    src/popup/             App.tsx with live task cards + Pause/Resume/Confirm
    manifest.config.ts + vite.config.ts
packages/
  shared-types/          WS protocol / ResilientSelector / canonical occupations / IDs
  skill-sdk/             SKILL.md manifest schema + loader
skills/
  eastmoney-news-digest/SKILL.md   (首发 · 金融分析)
  taobao-qianniu-inbox/SKILL.md    (Skill SDK 参考模板)
docs/
  HOLADAY_* / BROWSER_AGENT_*   (创始人交付的战略 / PoC / orchestrator 设计)
  STRATEGY_PIVOT_2026-04-21.md  (首发场景调整记录)
  CHROME_LOAD_GUIDE.md
  W1_DEMO.md                    (周五验收 runbook)
  W1_REHEARSAL.md               (彩排报告)
  daily/{2026-04-17 … 2026-04-22-w1}.md
```

### 测试矩阵（Day 7 终态）

| 类型 | 文件 | 项数 | 状态 |
|---|---|---|---|
| 单测 · orchestrator | `auth/password` / `auth/jwt` / `agent/task-controller` / `agent/planners/{stub,anthropic}` | **31** | ✅ |
| 单测 · skill-sdk | `loader.test` | **4** | ✅ |
| 集成 · orchestrator | `auth/service` / `redis` / `ws/server` / `ws/restart-recovery` / `agent/{task-repository, rehydrate, paused}` / `trpc/routers/tasks-confirm` | **14** | ✅ |
| **合计** | | **49** | **✅** |

额外验证：
- 真 Anthropic Haiku smoke（`pnpm smoke-anthropic`）✅
- 真 Anthropic Opus 4.7 × 6 次 Demo 彩排 ✅

---

## Milestone 1 已验收条目对照（设计文档 §Phase 0 功能验收）

| M1 条目 | 状态 | 证据 |
|---|---|---|
| 手机号注册登录 | 部分 ✅ | 邮箱 + argon2 + JWT 已实现（创始人决策调整为邮箱）。手机号/短信 Phase 1 |
| 5 分钟引导生成职业指纹 | 🗂️ 结构就位 | `user_profiles` 表 + `occupation_raw` / `canonical` / `fingerprint` JSON 字段；前端引导 UI 在 W2/W3 |
| 扩展装机 + WebSocket 连通 | ✅ | PoC A1 通过；`docs/CHROME_LOAD_GUIDE.md`；subprotocol + JWT 鉴权 + welcome + 30s 心跳 |
| 自然语言任务 → 司令层拆解 → 选中 Skill → 执行 | ✅ | 真 Opus 4.7 彩排 6/6 产合法 plan 7-11 步；skill catalogue 注入 slug+description；执行 = Phase 0 stub（W2 接 Playwright-CRX 后是真浏览器） |
| 实时可视化 | ✅ | popup 卡片随 step 状态刷新；状态 badge 5 色 |
| 高危动作弹窗确认 | ✅ | awaiting_user 三入口（client awaiting_user / requiresConfirm / isHighRisk）+ popup Confirm/Reject + tRPC `tasks.confirm` |
| 任务完成展示结果 + Web 历史查询 | 部分 ✅ | task_events 全量审计；`apps/web` 历史查询 UI 还没做（W5 周） |
| 3 个预装 Skill | 1/3 ⏳ | `eastmoney-news-digest` 入库 active；`douyin-compass-overview` 待 W2 商家授权方案；`taobao-qianniu-inbox` 保留为 SDK 模板（按战略 pivot 不计入首发 3 个） |
| Agent Loop 崩溃恢复 | ✅ | `rehydrateInFlight` + restart-recovery 集成测试 + 彩排 Demo C（+ 真进程重启由集成测试覆盖） |
| 任务完成率 > 60%（10 家种子企业） | ⏳ | Phase 0 dogfood 阶段衡量（W6-W8） |

### 非功能

| 条目 | 状态 | 证据 |
|---|---|---|
| WebSocket 断线 5s 内重连 | ✅ | SW 指数退避（cap 30s + jitter），**首次重连** 500ms 内（0.5 → 1 → 2 …） |
| 单动作延迟 p95 < 3s | ✅ 基建 | Phase 0 stub 步 500ms；真浏览器动作由 W2 测 |
| Orchestrator 7x24 不崩 | ✅ 基建 | pino 日志 / tsx watch 可靠重启 / shutdown 处理修过一次（idempotent） |
| 核心模块测试覆盖率 > 70% | ✅ | 49 项覆盖 agent / auth / ws / persistence / tRPC / 工具链 |

---

## 关键决策记录（W1 期间）

| 决策 | 时间 | 落地 |
|---|---|---|
| 手机号注册 → 邮箱 | Day 1 | `auth.register/login` 以邮箱为 key |
| 延迟实现真浏览器动作 | Day 6 | Phase 0 stub step execution；W2 接 Playwright-CRX |
| MAX_STEP_RETRIES=1（错一次重试一次，再错进 paused） | Day 5 | `task-controller.ts` 常量 |
| skill catalogue = slug + 一行 description（lazy-load body） | Day 5 | `PlannerContext.skills[]` + `AnthropicPlanner.buildSystemBlocks` |
| paused 三触发器：user / retries_exhausted / quota_exceeded | Day 5 | 完整落地 + 3 条集成测试 |
| 首发场景从淘宝 → 抖音 + 金融 | Day 6 | `docs/STRATEGY_PIVOT_2026-04-21.md`；`eastmoney-news-digest` 上线 |
| 放弃 `thinking + tool_choice` 组合 | Day 7 | Opus 4.7 API 不支持；彩排抓到；已去除 |
| Skill Manifest selector 走"permissive schema"（W2 driver 再严格） | Day 7 | `selectorStrategySchema` 从 discriminated union 降到 flat object |

---

## W2（04-28 周一起）启动计划

按设计文档 W2 目标：SkillLoader + CommanderLayer + LLM Router + 第一个 Skill 端到端。

### 必做（W2 目标范围）

1. **Playwright-CRX 适配层**（`packages/browser-driver/`）：替换 extension 里的 stub step 执行。`HolaDayBrowserDriver` 接口 + Playwright-CRX Adapter。
2. **SkillLoader**：commander 决定用 slug 后，按需 fetch `skills.manifest` 的 body（Markdown 正文）作为追加的 system 消息喂给下一步 action-plan。（v0.2 §5.5 lazy-load）
3. **真 end-to-end eastmoney-news-digest**：跑真的 goto/wait/extract，在本地 Chrome 里拿到真数据（不 stub）。
4. **第二个 skill**：`douyin-compass-overview`（抖音罗盘概览，商家侧）—— 需要先定商家账号授权方案，然后写 SKILL.md。
5. **`llm_calls` 表打通**（W2 backlog b2）：每次 AnthropicPlanner 调用写一行（tokens + cost + latency）。

### 周二前必做（backlog-quick）

- W2 backlog b1：重启后对 `executing` 任务重发 dispatch。彩排暴露的遗漏。
- W2 backlog b3：tool schema 的 selector description 更严格，降低 Opus 漏字段的概率。

### 周五前可做（backlog-stretch）

- W2 backlog b4：streaming plan（Opus 边产边送，降低首 token latency）。
- W2 backlog b5：真 Chrome 中 popup 的观察性彩排。

### W1 → W2 交接点

- `claude/chrome-extension-poc-biwr6` 分支维持到 W2 结束；W2 的结构性改动（browser-driver package）单独 PR 合回。
- 真 Opus 彩排脚本（`apps/orchestrator/.rehearsal/`）gitignored，founder 若要复用可直接拷贝一份放到自己机器上。

---

## 风险 / 需要决策

- ⚠️ **b1（executing 任务重启后没重发 dispatch）是 W2 Day 1 必修的**。真重启场景下这是个 correctness 洞（当前集成测试没覆盖，因为所有 restart-recovery 测试都走 awaiting_user 分支）。
- ⚠️ **抖音罗盘授权模型**：商家 Oauth vs 密码登录，决定 Skill 能不能用。建议 W2 第一周内敲定。
- ⚠️ **Phase 0 成本预算**：10 家 × 30 次/天 ≈ $225/月（Opus）。Prompt caching 打开后降 30%。Phase 1 前建议评估"用 Sonnet 4.6 跑 commander"的成本/质量 trade-off，可能 $225 → $135/月。

---

## 一句话总结

**W1 的承诺兑现**：Agent Loop 全状态机、持久化、崩溃恢复、paused 三触发器、真 Opus 规划、popup 用户控制面——49 项测试硬证据 + 6 次真 Opus 彩排全绿 + 3 个彩排期发现的 demo-killer 级 bug 已修。04-24 周五验收按 `docs/W1_DEMO.md` 跑即可。
