# HOLA DAY — Daily Report · 2026-04-17 (Day 1)

> **Branch**：`claude/chrome-extension-poc-biwr6`
> **Phase**：0 (W1 / Day 1 of roadmap)
> **Session 备注**：旧会话代码未迁移，Day 1 从零重建。

---

## 今日完成（5 项 + 1 通道）

| # | 任务 | Commit |
|---|---|---|
| 0 | 验证 push 通道到 `holaday-ai/holaday-monorepo` | — |
| 1 | pnpm monorepo 骨架（workspace + turbo + tsconfig + biome） | `d4a05e5` |
| 2 | docker-compose.yml（MySQL 8.4 + Redis 7.4 + MinIO） | `98890aa` |
| 3 | `apps/orchestrator/` 骨架（Express 5 + tRPC + ws + Drizzle） | `8825802` |
| 4 | DDL 第一稿（Drizzle schema，8 张表） | `09cd32f` |
| 5 | `packages/shared-types/`（WS 协议 + ResilientSelector + canonical occupations） | `bb33a86` |

全部通过 `pnpm -r typecheck` 和 `pnpm lint`，每任务独立 commit & push。

---

## 关键决策落地情况

| 决策 | 落地位置 | 状态 |
|---|---|---|
| ORM = Drizzle | `apps/orchestrator/src/db/` + drizzle-kit 0.31.10 | ✅ |
| 认证 = 邮箱 + argon2 + JWT(jose) | 依赖已装；`users.password_hash`；WS verifyToken 已用 jose | ✅ 结构 |
| external_id = NanoID(21) + 前缀 | `packages/shared-types/src/ids.ts`：`ID_PREFIXES` / `newExternalId()` / `isExternalId()` | ✅ |
| occupation 自由文本 + canonical list | `user_profiles.occupation_raw / occupation_canonical` + `CANONICAL_OCCUPATIONS` + `matchOccupation()` | ✅ |
| users.plan = VARCHAR(32) | `users.ts` `plan varchar(32) default 'free'`，附 `ix_users_plan` 索引 | ✅ |
| WS 鉴权 = Subprotocol + 首帧验证 | `ws/server.ts` `handleProtocols`（`holaday.v1`）+ JWT via `jwt.<token>` 子协议；若缺失，10 秒内必须发 `client.hello` 带 token | ✅ |
| task_events 按月分区 | 复合主键 (id, created_at)；手写迁移 `drizzle/manual/0001_partition_task_events.sql` 预建 4 个月 + `pmax` | ✅ 草案 |
| 心跳 30s / 超时 90s | `HEARTBEAT_INTERVAL_MS=30_000` / `HEARTBEAT_TIMEOUT_MS=90_000`；WeakMap 追踪 lastPingAt | ✅ |
| 一个 session 管所有 tab | `sessions` 表一行 = 一个扩展连接，tab 通过后续 `task_steps.input` 中的 tab_id 字段区分（不在 DB 建 tabs 表） | ✅ 约定 |

---

## 8 张表对照 Drizzle schema

```
users           9 列 / 3 idx / 0 fk
user_profiles   9 列 / 2 idx / 1 fk (user_id → users.id)
skills         12 列 / 4 idx / 0 fk
sessions       10 列 / 3 idx / 1 fk (user_id)
tasks          15 列 / 4 idx / 3 fk (user / session / skill)
task_steps     16 列 / 4 idx / 1 fk (task_id)
task_events     8 列 / 3 idx / 0 fk  (composite PK for partitioning)
llm_calls      18 列 / 4 idx / 0 fk
```

迁移文件：`apps/orchestrator/drizzle/0000_initial.sql`（自动生成）
分区迁移：`apps/orchestrator/drizzle/manual/0001_partition_task_events.sql`（需在有流量前手动执行）

---

## 遇到的问题 & 处理

1. **drizzle-kit 0.30.1 不识别 NodeNext 的 `.js` 重写** → 升级到 **0.31.10**，生成迁移通过。
2. **@trpc/server 11 要求 TS ≥ 5.7.2** → 根 + 两个子包的 `typescript` 统一升到 **5.7.3**。
3. **pnpm isolated 链接导致 TS2742（unnamed portable type）** → 在 orchestrator 的 `tsconfig.json` 关 `declaration`；`tsconfig.build.json` 打包时再开。

---

## 明日计划（Day 2）

1. 把 `apps/orchestrator` 的 Auth 流（email register/login + argon2 + JWT）接出 tRPC endpoint，单测覆盖。
2. 补 `apps/extension/` MV3 + Vite 骨架（PoC W1 里的 A1 目标）。
3. WS 协议增补：真正的 `client.step.result` → `server.task.control` 控制回路。
4. Orchestrator 启动集成测试（docker compose 拉起 MySQL + Redis，跑一次 migrate → 写一行 user → 查）。

---

## 风险 / 需要创始人决策

- ⚠️ **DDL 定稿**：本稿是 Day 1 草稿。明天跑通 migrate 后再发一版正式 DDL（定稿前任何列改动都零成本）。
- ⚠️ **WS 协议**：Hello/Welcome/Ping/Pong/Task 控制帧已经齐了，但 `client.step.result` 的 `data` 还是 `unknown`——要不要在 Phase 0 就把 3 个预装 Skill 的 result schema 也落在 shared-types 里？
- ⚠️ **分区维护策略**：现在预建 4 个月。之后是用 MySQL EVENT 自动加分区，还是 Manus 每月手动跑脚本？倾向前者，但需要你点头。
